[allowlist]
description = "Allowlist environment-specific placeholder config files (no real secrets). Keep minimal."
paths = [
  "Backend/src/BARQ.API/appsettings.Staging.json",
  "Backend/src/BARQ.API/appsettings.Production.json"
]
