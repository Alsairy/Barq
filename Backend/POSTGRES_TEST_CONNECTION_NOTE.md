No references to Username=root were found in the backend codebase, test configs, or .NET test setup. The GitHub Actions workflow for backend tests already uses Username=postgres and Password=postgres for the PostgreSQL service. The .NET test code uses an in-memory database for tests, so the PostgreSQL connection string is not used in .NET test runs. No changes were necessary for this issue.
